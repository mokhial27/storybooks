

<h6 style="font-family: 'Playfair Display', serif; font-size: 33px; font-weight: bold;  background-color: #00424a; padding: 7px; color: #ffffff;">dashboard</h6>
<h3 style="font-family: 'Playfair Display', serif; font-size: 35px; font-weight: bold;  background-color: #00424a; padding: 7px; color: #f5f5f5;">Welcome {{name}}!
<h1 style="font-family: 'Playfair Display', serif; font-size: 42px; font-weight: bold;  background-color: #00424a; padding: 7px; color: #f7f7f7;">Here is your stories:</h1>
{{#if stories}}
<table class="striped">
    <thead>
        <tr> 
            <th>Title</th>
            <th>Date</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each stories}}
        <tr>
            <td><a href="/stories/{{_id}}">{{title}}</a></td>
            <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm'}}</td>
            <td><span class="dash-status">{{status}}</span></td>
            <td>
                <a href="/stories/edit/{{_id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>

                <form action="/stories/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn red">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<p>You have not created any stories</p>
{{/if}}